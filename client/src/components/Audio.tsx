import React from "react";

interface Props {
    name: string;
    file: string;
}

const Audio = ({name, file}:Props) => {

    return (
        <div>
            Audio
        </div>
    )
}
export default Audio